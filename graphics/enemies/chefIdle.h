#include <SDL2/SDL.h>
#include <SDL2/SDL_image.h>
#include <stdio.h>
#include <string.h>

extern SDL_Rect CIdstrect;

void initChefIdle(SDL_Renderer* sdlRenderer);
void displayChefIdle(int x, int y);
void destroyChefIdle();
